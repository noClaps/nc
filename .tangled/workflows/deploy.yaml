when:
  - event: [push, manual]
    branch: [main]

engine: nixery

dependencies:
  nixpkgs:
    - nodejs
    - bun

steps:
  - name: Install dependencies
    command: bun install
  - name: Build the site
    command: bun run build
  - name: Deploy to Cloudflare
    command: bun x wrangler deploy
